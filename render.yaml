services:
  - type: web
    name: eventlogger
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: BASE_URL
        value: https://eventlogger-g6c5.onrender.com
